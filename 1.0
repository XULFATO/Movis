$ErrorActionPreference = "Stop"

# Ruta del Excel en red (ajusta a tu caso)
$file = "O:\ADP_SP\Clientes_Bob_LOPD\J18_ANCERT\01. Documentación Implantación\03.-ANALISIS\J18-Blueprint_v6.1 validado 09102025.xlsx"
$literal = "embargo"

try {
    $excel = New-Object -ComObject Excel.Application
    $excel.Visible = $false
    $excel.DisplayAlerts = $false

    Write-Host "📄 Abriendo Excel (solo lectura): $file" -ForegroundColor Cyan
    $wb = $excel.Workbooks.Open($file, 0, $true)  # read-only

    # Buscar hoja que contenga "analisis" y "conceptos"
    $sheet = $null
    foreach ($sh in $wb.Worksheets) {
        $name = $sh.Name.ToLower()
        if ($name -match "analisis" -and $name -match "concept") {
            $sheet = $sh
            break
        }
    }

    if (-not $sheet) {
        Write-Host "❌ No se encontró la hoja de Análisis Conceptos" -ForegroundColor Red
    } else {
        Write-Host "✅ Analizando hoja: $($sheet.Name)" -ForegroundColor Green
        $usedRange = $sheet.UsedRange
        $values = $usedRange.Value2

        $found = $false
        for ($r = 1; $r -le $values.GetLength(0); $r++) {
            for ($c = 1; $c -le $values.GetLength(1); $c++) {
                $val = $values[$r, $c]
                if ($null -ne $val -and ($val.ToString().ToLower() -like "*$literal*")) {
                    Write-Host "✅ Literal '$literal' encontrado en $($sheet.Name)!  Celda: R$r C$c" -ForegroundColor Green
                    $found = $true
                }
            }
        }

        if (-not $found) {
            Write-Host "⚪ No se encontró el literal '$literal' en la hoja." -ForegroundColor Gray
        }
    }

    $wb.Close($false)
}
catch {
    Write-Host "⚠️ Error: $($_.Exception.Message)" -ForegroundColor Yellow
}
finally {
    if ($excel) { $excel.Quit(); [System.Runtime.InteropServices.Marshal]::ReleaseComObject($excel) | Out-Null }
}